# Select
One sentence: labelled dropdown matching TextField styling, used for league/player pickers in the box-league score form.

```jsx
<Select label="Select league" placeholder="Select league…" options={[{value:'a',label:'Box A'},{value:'b',label:'Box B'}]} />
```
