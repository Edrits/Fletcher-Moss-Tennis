# TextField
One sentence: labelled input/textarea used across admin panels and the pairings paste box; green focus ring, no default browser chrome.

```jsx
<TextField label="Admin password" type="password" placeholder="Enter admin password" />
<TextField label="Noticeboard content" multiline placeholder="Type your update…" />
```
