# Banner
One sentence: full-width inline message strip — replaces the source's emoji-led rain alert (`☔ High rain risk...`) with an icon-led, softened-terracotta banner.

```jsx
<Banner tone="alert" icon={<Icon name="cloud-rain" size={16}/>}>High rain risk today — check WhatsApp for session updates.</Banner>
```
