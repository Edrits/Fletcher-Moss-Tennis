# SectionHeading
One sentence: the repeated kicker + serif `<h2>` pattern that opens every section on the site, now a single primitive instead of copy-pasted markup.

```jsx
<SectionHeading kicker="How it works" title="Getting on court" />
<SectionHeading title="Court Weather Check" inverse />
```
